# Deprecated: global versioning context removed in favor of per-command options.
# Keeping file to avoid import errors if referenced elsewhere.

def set_versioning(version_name, no_version):
    pass

def get_versioning():
    return None, False

